{
  "_id": "{{@ system @}}-{{@ datatype @}}-collect",
  "namespaced_identifiers": false,
  "source": {
    "operation": "{{@ datatype @}}-list",
    "system": "{{@ system @}}",
    "type": "rest"
  },
  "transform": {
    "rules": {
      "default": [
        [
          "merge",
          "_S.node"
        ]
      ]
    },
    "type": "dtl"
  },
  "type": "pipe"
}
